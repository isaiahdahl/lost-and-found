<nav class="navbar navbar-inverse navbar-static-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= pets_path %>">Lost and Found</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-right" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><a href="#">Lost Pets</a></li>
        <li><a href="<%= found_path %>">Found Pets</a></li>
        <li><a href="<%= new_pet_path %>">I lost my pet</a></li>
        <li><a href="#">pointless link</a></li>
      </ul>
      <form class="navbar-form navbar-right" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button href="www.google.com" type="submit" class="btn btn-success">Submit</button>
      </form>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<div class="banner header">
</div>
<div class="container">
  <div class="row">
    <% @pets.each do |pet|  %>
        <div class="col-xs-12 col-sm-4">
          <div class="card pets">
            <div class="card-block">
              <h3 class="card-title"><%= pet.name %></h3>
              <p class="card-text"><%= pet.category %></p>
              <p class="card-text"><%= pet.address %></p>
              <p class="card-text"><%= pet.found_on %></p>
              <a href="<%= pet_path(pet.id) %>" class="btn btn-primary bottomaligned">Read more</a>
            </div>
          </div>
        </div>
    <% end %>
  </div>
</div>